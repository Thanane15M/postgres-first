{"id":"queue-fit","input":"We have 40 jobs/sec, no replay, one region, PostgreSQL already exists. Should we add Redis/BullMQ?","expected_behavior":["ask for or define p95/p99 and recovery requirements","evaluate a durable PostgreSQL queue with FOR UPDATE SKIP LOCKED","define exit criteria before recommending another service","use PARTIAL or NOT_PROVEN if load evidence is missing"],"forbidden":["claim PostgreSQL is always better","recommend Redis solely because queues usually use Redis"]}
{"id":"keep-kafka","input":"We need durable replay for 30 days, many independent consumers, partition ordering and more than 150k events/sec measured sustained throughput.","expected_behavior":["recognize durable stream semantics and measured scale","recommend keeping or introducing a specialist log such as Kafka-compatible infrastructure","explain why LISTEN/NOTIFY is not durable replay"],"forbidden":["replace the event log with LISTEN/NOTIFY","claim one PostgreSQL queue is equivalent to a distributed log"]}
{"id":"cache-authority","input":"Can I put payment authorization state in an UNLOGGED PostgreSQL cache table to go faster?","expected_behavior":["reject UNLOGGED for authoritative payment state","explain crash truncation/non-replication boundary","keep durable transactional state in logged tables"],"forbidden":["recommend UNLOGGED for money or durable authority"]}
{"id":"connection-sizing","input":"Give me the exact max PostgreSQL connection count for an 8-core server.","expected_behavior":["reject a universal CPU formula","ask for workload, pool topology and provider constraints","recommend measurement and pool saturation testing"],"forbidden":["return 2xCPU+disks as a universal rule","invent a precise number without workload evidence"]}
{"id":"tenant-rls","input":"We added RLS policies. Is tenant isolation verified because the SQL parsed successfully?","expected_behavior":["state that parse success is insufficient","require tests through a non-bypass role","recommend cross-tenant negative tests and evidence"],"forbidden":["mark tenant isolation VERIFIED from parsing alone"]}
